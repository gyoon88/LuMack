<Window x:Class="LuMack.Views.PatchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LuMack.Views"
        xmlns:viewModels="clr-namespace:LuMack.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:PatchViewModel}"
        Title="Patch Verification" Height="600" Width="800"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: List and Controls -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button Content="Generate Patches" Margin="0,0,5,0" Command="{Binding GeneratePatchesCommand}"/>
                <Button Content="Save Selected" Command="{Binding SaveSelectedPatchesCommand}"/>
            </StackPanel>
            <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusText}" Foreground="Gray"/>
            <TextBlock DockPanel.Dock="Top" Margin="0,5,0,5" Foreground="White">
                <Run Text="Selected:"/>
                <Run Text="{Binding SelectedPatchCount}"/>
            </TextBlock>
            
            <ListBox ItemsSource="{Binding PatchPoints}"
                     SelectedItem="{Binding SelectedPatchPoint}"
                     HorizontalContentAlignment="Stretch"
                     Margin="0,5,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsSelectedToSave, Mode=TwoWay}"/>
                            <TextBlock Text="{Binding DisplayName}" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <!-- Right Panel: Image Display -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource PrimaryGrayBrush}" Margin="2">

                <Image Source="{Binding SelectedPatchPoint.MaskPatch}" Stretch="Uniform"/>
            </Border>
            
            <Border Grid.Row="1" Background="{StaticResource PrimaryGrayBrush}" Margin="2">
                <Image Source="{Binding SelectedPatchPoint.MaskPatch}" Stretch="Uniform"/>
            </Border>
        </Grid>
    </Grid>
</Window>
